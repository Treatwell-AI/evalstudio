# Generate Changelog

Generate a changelog entry based on recent commits and changes.

> Also available as `/feature changelog` (with feature context).

## Instructions

Read and execute the steps in `.claude/shared/changelog-steps.md`.

## Options

- `/changelog --dry-run` - Show what would be added without writing
- `/changelog "Custom message"` - Use custom description instead of auto-generating
