# Update Documentation

Update the `@evalstudio/docs` package with new feature documentation.

> Also available as `/feature docs` (with feature context).

## Instructions

Read and execute the steps in `.claude/shared/docs-steps.md`.
