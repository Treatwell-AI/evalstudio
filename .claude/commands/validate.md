# Validate Changes

Run all validation steps to ensure changes are ready for commit.

> Also available as `/feature validate` (with feature context).

## Instructions

Read and execute the steps in `.claude/shared/validation-steps.md`.

## Options

- `/validate --fix` - Run `yarn lint --fix` before validation
- `/validate --quick` - Skip build step (faster iteration)
